import { useState } from 'react';
import './App.css';

function App() {

  const [users, setUsers] = useState([])

  
  // function handleLogin(users) {
  //   setUsers(users);
  // }

  return (
    <div className="App">
    </div>
  );
}

export default App;
