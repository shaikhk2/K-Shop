

function ProfileContainer( { users }) {

    return (
        <div>
            {/* {users.map((user) => <ProfileCard
            key={user.id} user={user}/>)}   */}
        </div>
    )
}

export default ProfileContainer;
